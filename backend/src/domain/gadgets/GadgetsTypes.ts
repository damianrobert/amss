export enum GadgetType {
	DEVICE = 'device',
	SENSOR = 'sensor',
}

export enum DeviceType {
	AC = 'ac',
	FAN = 'fan',
	LIGHT_BULB = 'lightbulb',
	SMART_LIGHT = 'smartight',
	SOCKET = 'socket',
}

export enum SensorType {
	TEMPERATURE = 'temperature',
	HUMIDITY = 'humidity',
}
